{
  "name": "github-proxy",
  "version": "1.0.0",
  "description": "This worker provides a **safe GitHub-only proxy** (not an open proxy) so your CLI can fetch files from GitHub via your own domain.",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260114.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.59.1"
  }
}
