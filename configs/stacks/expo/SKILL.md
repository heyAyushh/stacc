---
name: expo
description: Route Expo application, EAS, native module, styling, deployment, and upgrade work to focused Expo skills. Use when building or operating an Expo or Expo Router project.
license: MIT
---

# Expo stack

Load only the matching child skill:

- App UI and data: `building-native-ui`, `native-data-fetching`, `use-dom`,
  `expo-tailwind-setup`, `expo-ui-swift-ui`, or
  `expo-ui-jetpack-compose`.
- Native integration: `expo-module` or `expo-brownfield`.
- Development and upgrades: `expo-dev-client` or `upgrading-expo`.
- Backend and delivery: `expo-api-routes`, `expo-deployment`,
  `expo-cicd-workflows`, or `eas-update-insights`.

Read the selected child `SKILL.md`; keep unrelated Expo references unloaded.
